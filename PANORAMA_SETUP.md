# 360° Панорамный Фон - Документация

## Что реализовано

### 🎨 VR-технология панорамного фона
Интегрирован 360° equirectangular панорамный фон за 3D моделью скутера с использованием технологии VR-очков.

## Компоненты

### 1. `PanoramaBackground.jsx`
**Расположение:** `/components/PanoramaBackground.jsx`

**Функционал:**
- Рендерит 360° панораму через Three.js
- Создает инвертированную сферу (видно изнутри)
- Применяет equirectangular mapping
- Синхронизируется с вращением 3D модели

**Props:**
```javascript
<PanoramaBackground 
  panoramaUrl="/images/studio-panorama.png"  // Путь к панорамному изображению
  rotation={0}                                // Угол вращения в радианах
  className="z-0"                             // CSS классы
/>
```

### 2. `ScooterViewer.jsx` (обновлен)
**Изменения:**
- Добавлен prop `panoramaUrl`
- Добавлено отслеживание вращения камеры через `camera-change` события
- `model-viewer` теперь с прозрачным фоном (`backgroundColor: 'transparent'`)
- Синхронизация угла вращения с панорамой через `setModelRotation()`

## Архитектура слоев

```
┌─────────────────────────────────────┐
│  z-20: Overlay эффект освещения     │  ← pointer-events: none
├─────────────────────────────────────┤
│  z-10: 3D модель скутера            │  ← pointer-events: auto, transparent bg
├─────────────────────────────────────┤
│  z-0:  Панорама 360°                │  ← pointer-events: none, вращается
└─────────────────────────────────────┘
```

## Файлы

### Панорамное изображение
**Путь:** `/public/images/studio-panorama.png`
**Размер:** 1.1MB
**Разрешение:** 1536×768 (2:1 aspect ratio для equirectangular)
**Источник:** Premium showroom environment

### Альтернативные панорамы
Также доступны:
- `/public/hdr/panoramic.png` (1536×768)
- `/public/images/panoramic.png` (1536×768)

## Как это работает

### 1. Загрузка панорамы
```javascript
const textureLoader = new THREE.TextureLoader()
textureLoader.load(panoramaUrl, (texture) => {
  const geometry = new THREE.SphereGeometry(500, 60, 40)
  geometry.scale(-1, 1, 1) // Инвертируем для просмотра изнутри
  
  const material = new THREE.MeshBasicMaterial({
    map: texture,
    side: THREE.BackSide, // Рендерим внутреннюю сторону
  })
  
  const sphere = new THREE.Mesh(geometry, material)
  scene.add(sphere)
})
```

### 2. Синхронизация вращения
```javascript
// В ScooterViewer.jsx
const handleCameraChange = () => {
  const orbit = modelViewer.getCameraOrbit()
  if (orbit && orbit.theta !== undefined) {
    const rotationRad = (orbit.theta * Math.PI) / 180
    setModelRotation(rotationRad) // Обновляет панораму
  }
}

modelViewer.addEventListener('camera-change', handleCameraChange)
```

### 3. Применение вращения к панораме
```javascript
// В PanoramaBackground.jsx
useEffect(() => {
  if (!sphereRef.current) return
  sphereRef.current.rotation.y = rotation // Вращаем сферу
  rendererRef.current.render(sceneRef.current, cameraRef.current)
}, [rotation])
```

## Интеграция в проект

### В `app/page.tsx`:
```javascript
<ScooterViewer
  modelPath={currentScooter.model}
  selectedDesign={selectedDesign}
  environmentImage="/hdr/studio.hdr"
  panoramaUrl="/images/studio-panorama.png"  // ← Добавлено
  className="w-full h-full"
/>
```

## Требования

### Зависимости
- `three` (уже установлен в проекте)
- `next/dynamic` для client-side рендеринга

### Формат панорамного изображения
- **Тип:** Equirectangular (2:1 aspect ratio)
- **Рекомендуемое разрешение:** 2048×1024 или выше
- **Форматы:** JPG, PNG, WebP
- **Оптимально:** 4096×2048 для высокого качества

## Производительность

### Оптимизации
1. **Lazy loading:** Компонент загружается только на клиенте через `dynamic()`
2. **Geometry detail:** 60×40 сегментов (баланс качества/производительности)
3. **Texture loading:** Асинхронная загрузка с fallback
4. **Render on demand:** Рендеринг только при изменении rotation
5. **Cleanup:** Правильная очистка Three.js ресурсов в useEffect cleanup

### Рекомендации
- Используйте оптимизированные изображения (WebP)
- Для мобильных устройств можно использовать меньшее разрешение
- Рассмотрите использование HDR для лучшего освещения

## Возможные улучшения

1. **Parallax эффект:** Добавить небольшое смещение панорамы при движении мыши
2. **Blur эффект:** Размытие дальних частей панорамы для глубины
3. **HDR поддержка:** Использование .hdr файлов для реалистичного освещения
4. **Adaptive quality:** Автоматическое снижение качества на слабых устройствах
5. **Multiple panoramas:** Разные панорамы для разных моделей скутеров

## Отладка

### Проверка загрузки панорамы
Откройте консоль браузера и проверьте:
```javascript
// Должны быть сообщения от Three.js
// Проверьте Network tab для загрузки изображения
```

### Проверка вращения
```javascript
// В консоли браузера:
console.log(modelViewer.getCameraOrbit())
// Должен выводить объект с theta, phi, radius
```

## Troubleshooting

### Панорама не отображается
- Проверьте путь к изображению в `panoramaUrl`
- Убедитесь, что файл существует в `/public/images/`
- Проверьте консоль на ошибки загрузки

### Панорама не вращается
- Убедитесь, что `camera-change` события срабатывают
- Проверьте, что `getCameraOrbit()` возвращает корректные данные
- Убедитесь, что `setModelRotation()` вызывается

### Низкая производительность
- Уменьшите разрешение панорамного изображения
- Снизите количество сегментов сферы (geometry)
- Используйте WebP вместо PNG

## Контакты и поддержка

Для вопросов и предложений по улучшению панорамного фона обращайтесь к разработчику проекта.



