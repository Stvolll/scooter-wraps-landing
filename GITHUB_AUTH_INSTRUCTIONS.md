# 🔐 Авторизация в GitHub и автоматический push

## 🚀 Быстрый способ (через GitHub CLI)

### Шаг 1: Установите GitHub CLI (если не установлен)

```bash
brew install gh
```

### Шаг 2: Авторизуйтесь

Выполните в терминале:

```bash
gh auth login
```

**Следуйте инструкциям:**
1. Выберите: **GitHub.com** (нажмите Enter)
2. Выберите: **HTTPS** (нажмите Enter)
3. Выберите: **Login with a web browser** (нажмите Enter)
4. Нажмите **Enter** для открытия браузера
5. В браузере нажмите **"Authorize github"**
6. Вернитесь в терминал

### Шаг 3: Настройте Git для использования GitHub CLI

```bash
git config --global credential.helper ""
git config --global credential.https://github.com.helper "!gh auth git-credential"
```

### Шаг 4: Выполните push

```bash
cd /Users/anatolii/scooter-wraps-landing
git push origin main
```

Теперь все будет работать автоматически! ✅

---

## 📝 Или используйте готовый скрипт

Я создал скрипт, который все сделает автоматически:

```bash
cd /Users/anatolii/scooter-wraps-landing
bash github_auth.sh
```

Скрипт:
- ✅ Проверит/установит GitHub CLI
- ✅ Авторизует вас в GitHub
- ✅ Настроит Git
- ✅ Выполнит push

---

## 🔍 Проверка авторизации

После авторизации проверьте:

```bash
gh auth status
```

Должно показать:
```
✓ Logged in to github.com as Stvolll
```

---

## ✅ После успешного push

Vercel автоматически задеплоит изменения через 1-3 минуты:
- ✅ **txd.bike** обновится
- ✅ **decalwrap.co** обновится

Проверьте статус: https://vercel.com/dashboard

---

## 🆘 Если что-то пошло не так

### Проблема: "Repository not found"
- Проверьте, существует ли репозиторий: https://github.com/Stvolll/scooter-wraps-landing
- Возможно, репозиторий находится в организации, а не у пользователя

### Проблема: "Permission denied"
- Убедитесь, что вы авторизованы: `gh auth status`
- Проверьте права доступа к репозиторию на GitHub

### Проблема: GitHub CLI не устанавливается
- Установите вручную: https://cli.github.com/
- Или используйте альтернативный способ с токеном (см. `QUICK_PUSH_SOLUTION.md`)



