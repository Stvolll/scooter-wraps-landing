# Руководство по быстрому старту

## 🚀 Начало работы

1. **Установите зависимости** (если еще не установлены):
   ```bash
   npm install
   ```

2. **Добавьте ваши 3D модели** в `/public/models/`:
   - `honda-vision.glb`
   - `honda-lead.glb` ✅ (уже существует)
   - `honda-sh.glb`
   - `honda-pcx.glb`
   - `yamaha-nvx.glb` ✅ (уже существует)

3. **Настройте дизайны** в `/config/scooters.js`:
   - Обновите названия дизайнов
   - Добавьте пути к текстурам или имена вариантов
   - При необходимости скорректируйте пути к моделям

4. **Запустите сервер разработки**:
   ```bash
   npm run dev
   ```

5. **Откройте** http://localhost:3000

## 📝 Конфигурация

### Добавление нового дизайна

Отредактируйте `/config/scooters.js`:

```javascript
designs: [
  { 
    id: '01', 
    name: 'Мой дизайн', 
    variant: 'my-design-variant'  // Для вариантов материалов
    // ИЛИ
    texture: '/textures/lead/my-design.png'  // Для замены текстур
  },
]
```

### Переключение между методами

**Варианты материалов (рекомендуется):**
- Экспортируйте GLB с вариантами в Blender
- Используйте свойство `variant` в конфиге
- Наиболее производительный метод

**Замена текстур:**
- Добавьте изображения текстур в `/public/textures/{model}/`
- Используйте свойство `texture` в конфиге
- Более гибкий, но менее производительный

**Отдельные файлы:**
- Создайте отдельный GLB для каждого дизайна
- Обновите путь `model` в конфиге дизайна
- Самый простой, но требует больше места

## 🎨 Настройка

### Изменение цветов
Отредактируйте `tailwind.config.ts`:
```typescript
accent: {
  neon: '#00ff88',  // Измените это
}
```

### Настройка скорости параллакса
Отредактируйте `app/page.tsx`:
```javascript
const parallaxY = scrollProgress * 100  // Измените множитель
```

### Изменение триггера прокрутки
Отредактируйте `app/page.tsx`:
```javascript
const triggerPoint = windowHeight * 0.8  // 80% области просмотра
```

## 🐛 Устранение неполадок

**Модель не загружается?**
- Проверьте путь к файлу в конфиге
- Убедитесь, что GLB существует в `/public/models/`
- Проверьте консоль браузера

**Дизайн не переключается?**
- Убедитесь, что имена вариантов совпадают с GLB
- Проверьте пути к файлам текстур
- Попробуйте другой метод переключения

**Проблемы с производительностью?**
- Оптимизируйте файлы GLB
- Используйте варианты материалов
- Сожмите текстуры

## 📚 Дополнительная информация

- См. `3D_HERO_SETUP.md` для подробной настройки
- См. `IMPLEMENTATION_SUMMARY.md` для обзора функций
- См. `/public/models/README.md` для требований к моделям
