# Настройка текстур дизайнов

## ✅ Что было сделано:

### 1. Обновлена конфигурация для Honda Lead

- Используются реальные изображения дизайнов из `/images/designs/honda lead/` для превью
- Используется текстура `/textures/honda-lead/3DModel.jpg` для применения на 3D модель
- 6 дизайнов теперь используют реальные изображения

### 2. Улучшено применение текстур

- Компонент теперь поддерживает:
  - **Material Variants** (рекомендуется) - через `variant` свойство
  - **Texture Swapping** - через `texture` свойство с доступом к Three.js сцене

## 📁 Структура файлов:

```
public/
├── images/designs/
│   └── honda lead/
│       ├── honda-lead-0.jpg  ← Фото дизайна 1 (для превью)
│       ├── honda-lead-1.jpg  ← Фото дизайна 2
│       └── ...
├── textures/
│   ├── honda-lead/
│   │   └── 3DModel.jpg       ← Текстура для 3D модели
│   └── yamaha-nvx/
│       └── 3DModel.jpg       ← Текстура для 3D модели
└── wraps/
    └── textures/              ← Папки для будущих текстур
```

## 🎨 Как добавить текстуры для дизайнов:

### Вариант 1: Material Variants (Рекомендуется)

1. Экспортируйте GLB модель с material variants в Blender:
   - Создайте материал для каждого дизайна
   - Назовите варианты (например: "design-1", "design-2")
   - Экспортируйте как GLB с вариантами

2. Обновите `config/scooters.js`:

```javascript
designs: [
  {
    id: '01',
    name: 'Design 1',
    variant: 'design-1', // Имя варианта из GLB
    preview: '/images/designs/honda lead/honda-lead-0.jpg',
  },
]
```

### Вариант 2: Отдельные текстуры

1. Создайте UV-текстуры для каждого дизайна (2048x2048 PNG)
2. Сохраните в `/public/textures/{model-id}/`:
   - `design-1.png`
   - `design-2.png`
   - и т.д.

3. Обновите `config/scooters.js`:

```javascript
designs: [
  {
    id: '01',
    name: 'Design 1',
    texture: '/textures/lead/design-1.png', // UV-текстура
    preview: '/images/designs/honda lead/honda-lead-0.jpg', // Фото для превью
  },
]
```

## 🔧 Текущая настройка:

- **Honda Lead**: Использует реальные фото для превью, `3DModel.jpg` для модели
- **Yamaha NVX**: Использует `3DModel.jpg` для модели
- **Другие модели**: Используют цветные заглушки

## 💡 Рекомендации:

1. **Для продакшена**: Создайте UV-текстуры для каждого дизайна или используйте material variants
2. **Фото дизайнов**: Используйте для превью в карточках (уже настроено)
3. **3D текстуры**: Должны соответствовать UV-развертке модели

## 📝 Следующие шаги:

1. Экспортируйте текстуры из Blender для каждого дизайна
2. Сохраните их в `/public/textures/{model-id}/`
3. Обновите конфигурацию с правильными путями
4. Протестируйте переключение дизайнов на 3D модели




