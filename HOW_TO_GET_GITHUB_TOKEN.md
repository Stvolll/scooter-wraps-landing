# 🔑 Как получить токен для GitHub (Personal Access Token)

## ⚠️ Важно!
GitHub **НЕ принимает обычный пароль** для push через HTTPS. Нужен **Personal Access Token (PAT)**.

---

## 📝 Пошаговая инструкция

### Шаг 1: Откройте страницу создания токена
Перейдите по ссылке: **https://github.com/settings/tokens**

Или вручную:
1. Откройте GitHub.com
2. Нажмите на ваш аватар (правый верхний угол)
3. Выберите **Settings**
4. В левом меню найдите **Developer settings**
5. Выберите **Personal access tokens** → **Tokens (classic)**

### Шаг 2: Создайте новый токен
1. Нажмите кнопку **"Generate new token"**
2. Выберите **"Generate new token (classic)"**

### Шаг 3: Настройте токен
1. **Note** (название): например, `scooter-wraps-landing` или `Mac Terminal`
2. **Expiration** (срок действия):
   - Рекомендуется: **90 days** или **No expiration** (если уверены в безопасности)
3. **Select scopes** (права доступа):
   - ✅ Обязательно отметьте: **`repo`** (полный доступ к репозиториям)
     - Это включает все подпункты:
       - `repo:status`
       - `repo_deployment`
       - `public_repo`
       - `repo:invite`
       - `security_events`

### Шаг 4: Создайте и скопируйте токен
1. Прокрутите вниз и нажмите **"Generate token"**
2. ⚠️ **ВАЖНО**: Токен показывается **только один раз**!
3. **Скопируйте токен** сразу (выглядит как: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)
4. Сохраните его в безопасном месте (например, в менеджере паролей)

---

## 🚀 Использование токена

### При запросе пароля в терминале:

```bash
Password for 'https://Stvolll@github.com':
```

**Введите:**
- **Username**: `Stvolll` (ваш GitHub username)
- **Password**: **вставьте скопированный токен** (не ваш обычный пароль!)

### Пример:
```
Username: Stvolll
Password: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## 🔒 Безопасность

- ✅ Токен = ваш пароль, **не делитесь им ни с кем**
- ✅ Не коммитьте токен в код
- ✅ Если токен скомпрометирован, удалите его и создайте новый
- ✅ Используйте менеджер паролей для хранения

---

## 🔄 Альтернатива: SSH ключ

Если не хотите использовать токены, можно настроить SSH:

1. Создайте SSH ключ:
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

2. Скопируйте публичный ключ:
```bash
cat ~/.ssh/id_ed25519.pub
```

3. Добавьте в GitHub: https://github.com/settings/keys

4. Измените remote URL на SSH:
```bash
git remote set-url origin git@github.com:Stvolll/scooter-wraps-landing.git
```

---

## ✅ После создания токена

Выполните push:
```bash
cd /Users/anatolii/scooter-wraps-landing
git push origin main
```

При запросе пароля вставьте токен.

---

**Готово!** 🎉



