# Переключатель языка (Language Switcher)

## ✅ Реализовано

Добавлен переключатель языка для переключения между вьетнамским (VI) и английским (EN) языками.

## 📁 Измененные файлы

1. **`contexts/LanguageContext.tsx`** - Новый контекст для управления языком
2. **`components/ClientWrapper.tsx`** - Обертка для LanguageProvider
3. **`hooks/useTranslations.ts`** - Обновлен для использования LanguageContext
4. **`components/Header.tsx`** - Добавлен переключатель языка
5. **`app/layout.tsx`** - Добавлен ClientWrapper

## 🎨 UI Переключателя

### Desktop версия
- Две кнопки: **VI** и **EN**
- Расположены в header справа, перед иконками контактов
- Активный язык выделен красным цветом (primary-600)
- Неактивный язык - белый фон с серым текстом

### Mobile версия
- Полноширинные кнопки: **Tiếng Việt** и **English**
- Расположены в мобильном меню
- Та же цветовая схема

## 🔧 Как это работает

1. **LanguageContext** хранит текущий язык в state
2. При изменении языка:
   - Обновляется state в контексте
   - Сохраняется в localStorage
   - Все компоненты, использующие `useTranslations()`, автоматически перерендериваются

3. **useTranslations hook**:
   - Использует `useLanguage()` из контекста
   - Возвращает функцию `t()` для переводов
   - Возвращает `lang` и `setLang` для управления языком

## 📝 Использование

```tsx
import { useTranslations } from '@/hooks/useTranslations'

function MyComponent() {
  const { t, lang, setLang } = useTranslations()
  
  return (
    <div>
      <h1>{t('hero.title')}</h1>
      <button onClick={() => setLang('en')}>English</button>
      <button onClick={() => setLang('vi')}>Tiếng Việt</button>
    </div>
  )
}
```

## 🌐 Поддерживаемые языки

- **vi** - Tiếng Việt (Вьетнамский) - язык по умолчанию
- **en** - English (Английский)

## 💾 Сохранение выбора

Выбранный язык сохраняется в `localStorage` с ключом `lang` и автоматически загружается при следующем посещении сайта.

## 🔄 Автоматическое обновление

Все компоненты, использующие `useTranslations()`, автоматически обновляются при изменении языка благодаря React Context API.

