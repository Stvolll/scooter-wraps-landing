# Автоматическая проверка и исправление ошибок

## 🛠️ Инструменты

### 1. `scripts/auto-fix.js`

Автоматическая проверка кода на распространенные ошибки:

```bash
node scripts/auto-fix.js
```

**Проверяет:**

- ✅ Порядок хуков React (все хуки должны вызываться до условных возвратов)
- ✅ Наличие `ssr: false` в dynamic import
- ✅ Наличие `suppressHydrationWarning` на проблемных элементах
- ✅ Наличие скрипта model-viewer в layout

### 2. `scripts/watch-and-fix.sh`

Мониторинг в реальном времени (опционально):

```bash
./scripts/watch-and-fix.sh
```

## 🔧 Автоматические исправления

Скрипт автоматически проверяет и сообщает о проблемах. Основные исправления уже применены:

1. **Порядок хуков** - все хуки вызываются до условных возвратов
2. **Dynamic import** - компонент загружается только на клиенте
3. **Hydration protection** - добавлены suppressHydrationWarning
4. **Script loading** - model-viewer загружается правильно

## 📋 Ручная проверка

Если ошибки все еще возникают:

1. **Очистите кеш браузера**: Ctrl+Shift+R (или Cmd+Shift+R)
2. **Перезапустите сервер**:
   ```bash
   # Остановите (Ctrl+C)
   npm run dev
   ```
3. **Проверьте консоль браузера** (F12) на другие ошибки

## ✅ Текущий статус

Все автоматические проверки пройдены успешно!
