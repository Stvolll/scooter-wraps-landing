# 🚀 Быстрое решение для push

## Проблема
- Терминал скрывает ввод (это нормально!)
- "Repository not found" или "Invalid username or token"

## ✅ Решение 1: Сохранить токен в Git (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Создайте токен
1. Откройте: https://github.com/settings/tokens
2. "Generate new token (classic)"
3. Отметьте `repo`
4. Скопируйте токен (начинается с `ghp_...`)

### Шаг 2: Сохраните токен в Git

Выполните в терминале (замените `YOUR_TOKEN` на ваш токен):

```bash
cd /Users/anatolii/scooter-wraps-landing

git credential approve <<EOF
protocol=https
host=github.com
username=Stvolll
password=YOUR_TOKEN
EOF
```

**Пример:**
```bash
git credential approve <<EOF
protocol=https
host=github.com
username=Stvolll
password=ghp_abc123xyz789...
EOF
```

### Шаг 3: Выполните push

```bash
git push origin main
```

Теперь Git автоматически использует сохраненный токен! ✅

---

## ✅ Решение 2: Push с токеном в URL (одноразово)

Если не хотите сохранять токен, используйте его прямо в команде:

```bash
cd /Users/anatolii/scooter-wraps-landing

git push https://YOUR_TOKEN@github.com/Stvolll/scooter-wraps-landing.git main
```

**Пример:**
```bash
git push https://ghp_abc123xyz789...@github.com/Stvolll/scooter-wraps-landing.git main
```

⚠️ **Внимание:** Токен будет виден в истории команд! Используйте только для одноразового push.

---

## ✅ Решение 3: GitHub CLI

Если установлен GitHub CLI:

```bash
# Авторизация
gh auth login

# Push
cd /Users/anatolii/scooter-wraps-landing
git push origin main
```

Установка GitHub CLI (если нет):
```bash
brew install gh
```

---

## ✅ Решение 4: Проверить организацию

Если репозиторий находится в организации (не у пользователя), нужно изменить URL:

```bash
# Проверьте правильный URL репозитория на GitHub
# Затем измените:
git remote set-url origin https://github.com/ORGANIZATION_NAME/scooter-wraps-landing.git

# И повторите push
git push origin main
```

---

## 🔍 Проверка текущего remote

Чтобы проверить текущий URL:

```bash
git remote get-url origin
```

---

## ❓ Какой вариант выбрать?

**Рекомендую Решение 1** - токен сохранится один раз, и больше не нужно будет его вводить.

---

## 🆘 Если ничего не помогает

1. Проверьте, существует ли репозиторий: https://github.com/Stvolll/scooter-wraps-landing
2. Проверьте права доступа к репозиторию
3. Убедитесь, что токен имеет права `repo`
4. Попробуйте создать новый токен

