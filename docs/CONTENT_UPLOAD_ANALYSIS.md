# Анализ системы загрузки контента

**Дата анализа:** 2024-12-10  
**Версия системы:** Design Lifecycle System v1.0

---

## 📊 ТЕКУЩАЯ СТРУКТУРА

### ✅ Что работает:

1. **Базовая загрузка файлов:**
   - Cover Image (обложка)
   - 3D Model (GLB)
   - Texture (текстура для 3D)
   - Gallery Images (массив изображений)

2. **Инфраструктура:**
   - S3 интеграция через signed URLs
   - Валидация типов файлов и размеров
   - Компонент `FileUpload` для UI
   - API endpoint `/api/uploads/signed-url`

3. **Хранение в БД (Prisma):**
   ```prisma
   model Design {
     coverImage       String?      // S3 URL
     galleryImages    String[]     // Массив S3 URL
     glbModelUrl      String?      // S3 URL
     textureUrl       String?      // S3 URL
     videoPreview     String?      // S3 URL (есть поле, но нет загрузки)
   }
   ```

### ⚠️ ПРОБЛЕМЫ И ОГРАНИЧЕНИЯ:

#### 1. **Отсутствие пакетной загрузки**
   - Каждый файл загружается отдельно
   - Нет связи между файлами одного дизайна
   - Нет возможности загрузить все файлы одним действием
   - Нет валидации полноты набора файлов

#### 2. **Неполная поддержка типов контента**
   - ❌ Видео (есть поле `videoPreview`, но нет UI для загрузки)
   - ❌ Векторная модель для раздела "How to Apply" (SVG/PDF)
   - ❌ Множественные текстуры для одной модели
   - ❌ Свойства 3D модели (освещение, камера, материалы)
   - ❌ Альтернативные форматы (KTX2, Basis Universal)

#### 3. **Отсутствие метаданных 3D модели**
   - Нет сохранения позиции камеры
   - Нет сохранения настроек освещения
   - Нет сохранения свойств материалов
   - Нет управления слоями текстур

#### 4. **Нет связи с разделом "How to Apply"**
   - Раздел "How to Apply" использует статичные данные
   - Нет возможности загрузить векторную схему для каждого дизайна
   - Нет привязки инструкций к конкретному дизайну

#### 5. **Ограниченная валидация**
   - Нет проверки соответствия текстуры модели
   - Нет проверки разрешения изображений
   - Нет проверки оптимизации 3D моделей
   - Нет предпросмотра перед сохранением

---

## 🎯 ТРЕБУЕМЫЙ КОНТЕНТ ДЛЯ ОДНОЙ ПОЗИЦИИ (ДИЗАЙНА)

### 1. **3D Модель и связанные файлы:**
   - ✅ GLB файл модели
   - ✅ Текстура (одна или несколько)
   - ❌ **Свойства модели:**
     - Позиция камеры (camera position)
     - Настройки освещения (lighting)
     - Свойства материалов (material properties)
     - Слои текстур (texture layers)
   - ❌ **Альтернативные форматы:**
     - Сжатая версия (Draco)
     - Мобильная версия (упрощенная)
     - KTX2 текстуры

### 2. **Изображения:**
   - ✅ Cover Image (обложка)
   - ✅ Gallery Images (галерея)
   - ❌ **Дополнительные:**
     - Thumbnail (миниатюра)
     - Social media preview
     - Print-ready версии

### 3. **Видео:**
   - ❌ Video Preview (превью видео)
   - ❌ Full Video (полное видео)
   - ❌ Tutorial Video (видео-инструкция)

### 4. **Векторная модель для "How to Apply":**
   - ❌ SVG схема разметки
   - ❌ PDF инструкция
   - ❌ Интерактивные точки (hotspots)
   - ❌ Аннотации и подписи

### 5. **Дополнительные файлы:**
   - ❌ Техническая документация
   - ❌ Файлы для печати (CMYK)
   - ❌ Шаблоны для дизайнеров

---

## 🔧 ПРЕДЛАГАЕМОЕ РЕШЕНИЕ

### Вариант 1: Расширение текущей схемы (Рекомендуется)

#### 1.1. Расширение Prisma Schema

```prisma
model Design {
  // ... существующие поля ...
  
  // 3D Model Assets
  glbModelUrl        String?
  glbModelCompressed String?  // Draco compressed version
  glbModelMobile     String?  // Simplified mobile version
  
  // Textures (multiple)
  textures           DesignTexture[]
  
  // 3D Model Properties
  modelProperties    DesignModelProperties?
  
  // Video
  videoPreview       String?
  videoFull          String?
  videoTutorial      String?
  
  // Vector/Blueprint for "How to Apply"
  blueprintSvg       String?  // SVG схема
  blueprintPdf       String?  // PDF инструкция
  blueprintHotspots  Json?    // Интерактивные точки
  
  // Additional assets
  thumbnail          String?
  socialPreview      String?
}

model DesignTexture {
  id          String   @id @default(cuid())
  design      Design   @relation(fields: [designId], references: [id], onDelete: Cascade)
  designId    String
  url         String   // S3 URL
  type        String   // "diffuse" | "normal" | "roughness" | "metallic" | "ao"
  format      String   // "png" | "jpg" | "ktx2" | "basis"
  resolution  String?  // "2048x2048"
  layer       Int      @default(0) // Слой текстуры
  createdAt   DateTime @default(now())
  
  @@index([designId])
}

model DesignModelProperties {
  id              String   @id @default(cuid())
  design          Design   @relation(fields: [designId], references: [id], onDelete: Cascade)
  designId        String   @unique
  
  // Camera
  cameraPosition  Json?    // { x, y, z, rotation }
  cameraTarget    Json?    // { x, y, z }
  
  // Lighting
  lighting        Json?    // { ambient, directional, shadows }
  
  // Materials
  materials       Json?    // { metalness, roughness, emissive }
  
  // Environment
  environmentMap  String?  // HDRI URL
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

#### 1.2. Пакетный API для загрузки

```typescript
// app/api/designs/[id]/assets/batch/route.ts
POST /api/designs/[id]/assets/batch
{
  files: [
    { type: "cover", file: File },
    { type: "glb", file: File },
    { type: "texture", file: File, metadata: { type: "diffuse", layer: 0 } },
    { type: "video", file: File, metadata: { type: "preview" } },
    { type: "blueprint", file: File, metadata: { type: "svg" } }
  ],
  modelProperties: {
    cameraPosition: { x, y, z },
    lighting: { ... },
    materials: { ... }
  }
}
```

#### 1.3. Единый компонент пакетной загрузки

```typescript
// components/BatchUpload.tsx
- Drag & Drop зона
- Предпросмотр всех файлов
- Валидация перед загрузкой
- Прогресс загрузки для каждого файла
- Связь файлов с дизайном
```

---

### Вариант 2: Отдельная модель Asset (Более гибкий)

Создать отдельную модель `DesignAsset` для всех типов файлов:

```prisma
model DesignAsset {
  id          String   @id @default(cuid())
  design      Design   @relation(fields: [designId], references: [id], onDelete: Cascade)
  designId    String
  type        String   // "cover" | "gallery" | "glb" | "texture" | "video" | "blueprint" | ...
  category    String   // "image" | "model" | "video" | "vector" | "document"
  url         String   // S3 URL
  metadata    Json?    // Дополнительные данные (разрешение, формат, размер)
  order       Int      @default(0) // Порядок отображения
  createdAt   DateTime @default(now())
  
  @@index([designId])
  @@index([type])
}
```

---

## 📋 ПЛАН РЕАЛИЗАЦИИ

### Этап 1: Анализ и проектирование ✅
- [x] Анализ текущей системы
- [x] Определение требований
- [x] Согласование решения

### Этап 2: Расширение схемы БД ✅
- [x] Обновить Prisma schema
- [x] Создать миграцию
- [x] Обновить типы TypeScript

### Этап 3: API для пакетной загрузки ✅
- [x] Создать `/api/designs/[id]/assets/batch`
- [x] Добавить валидацию
- [x] Реализовать параллельную загрузку
- [x] Создать `/api/designs/[id]/model-properties`

### Этап 4: Компонент пакетной загрузки ✅
- [x] Создать `BatchUpload` компонент
- [x] Drag & Drop интерфейс
- [x] Предпросмотр и валидация
- [x] Прогресс загрузки
- [x] Подсказки на русском языке

### Этап 5: Управление свойствами 3D ✅
- [x] UI для настройки камеры
- [x] UI для настройки освещения
- [x] Сохранение свойств материалов
- [x] API для сохранения/загрузки свойств

### Этап 6: Интеграция с "How to Apply" 🔄
- [x] Загрузка векторных схем (SVG, PDF)
- [ ] Редактор hotspots (в разработке)
- [x] Привязка к дизайну

---

## 🎨 UI/UX ПРЕДЛОЖЕНИЯ

### Единый интерфейс загрузки:

```
┌─────────────────────────────────────────┐
│  📦 Пакетная загрузка контента          │
├─────────────────────────────────────────┤
│                                         │
│  [Drag & Drop зона]                    │
│  или                                    │
│  [Выбрать файлы]                        │
│                                         │
│  ┌─────────────────────────────────┐  │
│  │ 📁 Загруженные файлы:            │  │
│  │                                   │  │
│  │ ✅ cover-image.webp               │  │
│  │ ✅ model.glb                      │  │
│  │ ✅ texture-diffuse.png            │  │
│  │ ✅ texture-normal.png             │  │
│  │ ⏳ video-preview.mp4 (загрузка)  │  │
│  │ ❌ blueprint.svg (ошибка)         │  │
│  └─────────────────────────────────┘  │
│                                         │
│  ┌─────────────────────────────────┐  │
│  │ ⚙️ Свойства 3D модели:           │  │
│  │                                   │  │
│  │ Камера: [Настроить]              │  │
│  │ Освещение: [Настроить]           │  │
│  │ Материалы: [Настроить]           │  │
│  └─────────────────────────────────┘  │
│                                         │
│  [Загрузить все] [Отмена]               │
└─────────────────────────────────────────┘
```

---

## 🔍 ВОПРОСЫ ДЛЯ УТОЧНЕНИЯ

1. **Векторная модель для "How to Apply":**
   - Какой формат предпочтителен? (SVG, PDF, JSON)
   - Нужны ли интерактивные точки (hotspots)?
   - Должна ли быть привязана к конкретному дизайну или общая для модели скутера?

2. **Свойства 3D модели:**
   - Как получать позицию камеры? (через UI или автоматически?)
   - Какие настройки освещения нужны?
   - Нужны ли слои текстур или достаточно одной?

3. **Видео:**
   - Какие типы видео нужны? (превью, полное, tutorial)
   - Максимальный размер?
   - Формат? (MP4, WebM)

4. **Приоритеты:**
   - Что реализовать в первую очередь?
   - Что можно отложить?

---

**Автор анализа:** AI Assistant  
**Следующий шаг:** Согласование решения и начало реализации

