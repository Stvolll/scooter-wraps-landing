# 🚀 Cursor Quick Actions - Готовые промпты для типичных задач

Этот файл содержит готовые промпты для быстрого выполнения типичных задач в проекте TXD Scooter Wraps Landing.

---

## 🎨 UI Компоненты

### Создать новый UI компонент
```
Создай компонент [ComponentName] в components/ui/[ComponentName].tsx:
- Используй iOS 26 Design System (glassmorphism)
- Добав hover states и анимации (framer-motion)
- Поддержка i18n для всех текстов
- TypeScript с strict typing (без any)
- Responsive дизайн (mobile-first)
- Следуй паттернам из существующих компонентов
```

### Создать секцию лендинга
```
Создай секцию [SectionName] в components/sections/[SectionName].tsx:
- Server Component по умолчанию
- Используй iOS 26 стиль (glassmorphism, градиенты)
- Поддержка всех 3 языков (en, vi, ko)
- Анимации через framer-motion
- Responsive layout
- Следуй структуре из docs/DESIGN_SYSTEM.md
```

---

## 🎮 3D Компоненты

### Создать Three.js компонент
```
Создай Three.js компонент в components/3d/[ComponentName].tsx:
- Dynamic import с loading state
- Обязательный cleanup Three.js объектов в useEffect cleanup
- Поддержка переключения текстур/материалов
- Оптимизация для мобильных устройств
- Draco compression для GLB моделей
- Error handling и fallback UI
- TypeScript типизация (без any)
```

### Интегрировать 3D модель
```
Интегрируй 3D модель [model-name].glb в компонент [ComponentName]:
- Используй ScooterViewer3D как референс
- Добав поддержку переключения текстур из selectedDesign
- Оптимизируй для мобильных (упрощенная версия)
- Добав loading state и error handling
- Cleanup всех Three.js ресурсов
```

---

## 🔄 Рефакторинг

### Оптимизировать компонент
```
Оптимизируй компонент [ComponentName] для производительности:
- Используй React.memo если нужно
- Примени useCallback/useMemo для тяжелых вычислений
- Проверь memory leaks (особенно Three.js объекты)
- Проверь bundle size
- Следуй docs/ARCHITECTURE_PRINCIPLES.md
- Убедись что нет лишних re-renders
```

### Конвертировать в Server Component
```
Конвертируй [ComponentName] в Server Component:
- Удали 'use client' если не нужны хуки
- Перенеси client-side логику в отдельный Client Component
- Используй async/await для data fetching
- Следуй Next.js 14 App Router паттернам
```

---

## 🐛 Исправление ошибок

### TypeScript ошибка
```
Почини TypeScript ошибку в [файл]:
[вставить полную ошибку]

Требования:
- Без использования `any`
- Следуй существующим паттернам проекта
- Проверь через `npm run build`
- Добав правильную типизацию
```

### Build ошибка
```
Исправь build ошибку:
[вставить ошибку из npm run build]

Требования:
- Проверь все импорты
- Убедись что все типы корректны
- Проверь что нет circular dependencies
- Запусти `npm run build` после исправления
```

### Runtime ошибка
```
Исправь runtime ошибку в [компонент/страница]:
[описание ошибки и stack trace]

Требования:
- Добав proper error handling
- Проверь что все async операции обработаны
- Убедись что нет undefined/null access
- Добав fallback UI если нужно
```

---

## 🌐 i18n (Интернационализация)

### Добавить переводы для новой фичи
```
Добавь переводы для новой фичи [FeatureName]:
- Добав ключи в locales/en.json, locales/vi.json, locales/ko.json
- Используй структуру: featureName.keyName
- Убедись что все тексты переведены
- Проверь что компонент использует useTranslations hook
```

### Исправить отсутствующие переводы
```
Найди и исправь все хардкоженные тексты в [компонент/страница]:
- Замени на ключи из locales/*.json
- Добав недостающие ключи во все 3 языка
- Используй useTranslations hook
- Проверь что все тексты отображаются корректно
```

---

## 🗄️ База данных

### Создать миграцию Prisma
```
Создай Prisma миграцию для [описание изменений]:
- Измени schema.prisma
- Создай миграцию через `npx prisma migrate dev --name [name]`
- Обнови типы через `npx prisma generate`
- Проверь что миграция работает корректно
```

### Обновить Design Lifecycle
```
Обновить Design Lifecycle для дизайна [designId]:
- Текущий статус: [currentStatus]
- Новый статус: [newStatus]
- Валидируй переход (нельзя пропускать этапы)
- Обнови statusHistory
- Добав note если нужно
```

---

## 🔐 Безопасность

### Добавить валидацию API endpoint
```
Добав валидацию для API endpoint [route]:
- Используй Zod schema для валидации
- Добав rate limiting
- Проверь CSRF защиту
- Добав proper error handling (не раскрывай детали)
- Следуй docs/SECURITY.md
```

### Исправить security issue
```
Исправь security issue в [файл/endpoint]:
[описание проблемы]

Требования:
- Убедись что нет секретов в коде
- Добав валидацию входных данных
- Проверь что нет SQL injection / XSS
- Следуй docs/SECURITY.md
```

---

## 🚀 Производительность

### Оптимизировать изображения
```
Оптимизируй изображения для [компонент/страница]:
- Используй next/image для всех изображений
- Добав responsive sizes
- Конвертируй в WebP/AVIF
- Используй lazy loading
- Проверь через npm run image:build
```

### Оптимизировать bundle size
```
Оптимизируй bundle size для [компонент/страница]:
- Используй dynamic imports для тяжелых компонентов
- Проверь что нет дублирующихся зависимостей
- Используй tree shaking
- Проверь bundle analyzer
```

---

## 📱 Адаптивность

### Сделать компонент responsive
```
Сделай компонент [ComponentName] полностью responsive:
- Mobile-first подход
- Breakpoints: sm (640px), md (768px), lg (1024px), xl (1280px)
- Тестируй на разных размерах экрана
- Убедись что touch interactions работают
- Проверь что нет горизонтального скролла
```

---

## 🧪 Тестирование

### Проверить компонент перед коммитом
```
Проверь компонент [ComponentName] перед коммитом:
1. npm run build - без TypeScript ошибок
2. npm run lint - без линтер ошибок
3. Проверь на мобильном устройстве
4. Проверь все 3 языка (en, vi, ko)
5. Проверь что нет console errors
6. Проверь производительность (Lighthouse)
```

---

## 📝 Документация

### Обновить документацию
```
Обновить документацию для [фича/компонент]:
- Добав описание в соответствующий файл в docs/
- Обнови примеры использования
- Добав типичные проблемы и решения
- Обнови архитектурную документацию если нужно
```

---

## 🔍 Поиск и анализ

### Найти все использования паттерна
```
Найди все места где используется [паттерн/компонент/функция]:
- Покажи все файлы где это используется
- Покажи контекст использования
- Предложи рефакторинг если нужно
```

### Анализ производительности
```
Проанализируй производительность [компонент/страница]:
- Проверь количество re-renders
- Проверь bundle size
- Проверь время загрузки
- Предложи оптимизации
```

---

## 💡 Советы по использованию

1. **Копируй промпт полностью** - включая все требования
2. **Заменяй [заполнители]** на реальные значения
3. **Добавляй контекст** - если есть специфичные требования
4. **Проверяй результат** - всегда запускай `npm run build` после изменений

---

## 🆘 Если что-то не работает

1. Проверь что ты следуешь `.cursorrules`
2. Проверь документацию в `docs/`
3. Посмотри на существующие примеры в коде
4. Запусти `npm run build` для проверки ошибок






